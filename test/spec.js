import { describe, it } from 'node:test';
import { strict as assert } from 'node:assert';

describe('npm-bump', () => {
    it('dummy test', () => {
        assert.strictEqual(true, true, 'dummy assertion');
    });
});
